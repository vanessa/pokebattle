import React from 'react';
import ReactLoading from 'react-loading';

const Loading = () => (
  <ReactLoading type="spin" color="#ccc" height={70} width={70} className="centered-spinner" />
);

export default Loading;
