import React from 'react';
import ReactLoading from 'react-loading';

const Loading = () => (
  <ReactLoading type="spin" color="#fff" height={90} width={90} className="centered-spinner" />
);

export default Loading;
